import React, {useState} from 'react'

import './Navbar.css'

const Navbar = () =>{
    const Tabs = ['Home', '']
    return(
        <nav>
            <div id = 'logout'> Log Out</div>
        </nav>
    )
}

export default Navbar;